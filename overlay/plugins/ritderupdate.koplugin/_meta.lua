local _ = require("gettext")
return {
    name = "ritderupdate",
    fullname = _("Ritder updates"),
    description = _([[Checks GitHub Releases for a newer Ritder and installs it over the air.]]),
}
